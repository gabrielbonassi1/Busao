'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
var rgba = require('color-rgba');

var colorDiff = exports.colorDiff = function colorDiff(color1, color2) {
    var colors = [rgba(color1), rgba(color2)];
    var diff = 0;
    for (var i = 0; i < 3; i++) {
        diff += (colors[0][i] - colors[1][i]) * (colors[0][i] - colors[1][i]);
    }return Math.sqrt(diff);
};

var maxDiff = exports.maxDiff = colorDiff('#000', '#fff');

var similarity = exports.similarity = function similarity(color1, color2) {
    return 1 - colorDiff(color1, color2) / maxDiff;
};

var isSimilar = exports.isSimilar = function isSimilar(color1, color2) {
    var confidence = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 0.8;

    return similarity(color1, color2) > confidence;
};